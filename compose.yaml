services:
  forgeflow:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
      args:
        APP_VERSION: local
        VCS_REF: local
    image: forgeflow:local
    environment:
      FORGEFLOW_VERSION: local
    ports:
      - "8080:8080"
    restart: unless-stopped
